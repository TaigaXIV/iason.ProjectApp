<Page
    x:Class="iason.ProjectApp.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iason.ProjectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel VerticalAlignment="Center">
        <TextBlock FontSize="24" FontWeight="Bold" Text="User Login:" Margin="0,0,0,20" HorizontalAlignment="Center" />
        <TextBox Header="Email:" Width="250" HorizontalAlignment="Center" Text="{x:Bind ViewModel.Email, Mode=TwoWay}"/>
        <PasswordBox Header="Password" Width="250" Margin="20" HorizontalAlignment="Center" Password="{x:Bind ViewModel.Password, Mode=TwoWay}"/>
        <Button x:Name="LoginButton" FontSize="18" Content="Login" Click="{x:Bind ViewModel.SubmitLogin, Mode=OneWay}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
